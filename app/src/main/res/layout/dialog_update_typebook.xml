<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:orientation="vertical">
    <EditText
        android:id="@+id/edtUpdateNameTypeBook"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Update typebook name"
        android:textSize="18sp"
        android:layout_marginBottom="10dp"
        android:inputType="text"
        android:imeOptions="actionNext"/>
    <EditText
        android:id="@+id/edtUpdatetDesTypebook"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Update typebook description "
        android:textSize="18sp"
        android:layout_marginBottom="10dp"
        android:inputType="text"
        android:imeOptions="actionNext"/>
    <EditText
        android:id="@+id/edtUpdatetPositionTypebook"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Update position description "
        android:textSize="18sp"
        android:inputType="text"
        android:imeOptions="actionDone"/>

</LinearLayout>